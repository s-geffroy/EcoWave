\section{Results}
\label{sec:results}

We apply the protocol to every cell of the
\(\{\text{Kitchin, Juglar, Kuznets, Kondratieff}\}\times\{\text{six panels}\}\)
grid. Gate~\ref{gate:3} cross-aggregate concordance is evaluated
on the WB panel whose income aggregates are well-defined. The
per-variable safeguard (Section~\ref{sec:safeguard}) is enforced
throughout. A heatmap of the dual-null
\(p\)-values on the full
\(\{\text{cycle}\}\times\{\text{panel}\}\) grid is available in
\path{reports/cycle_position_v1.pdf} for the reader who wishes to
inspect cell-level outcomes; the discussion below summarises the
verdict.

\subsection{Headline finding}

No canonical periodicity from the
\{Kitchin, Juglar, Kuznets, Kondratieff\} taxonomy passes the
uniform protocol on more than a small minority of panels. The
finding is qualitatively invariant under Benjamini-Hochberg
correction at FDR 0.05 (Appendix~\ref{app:per_variable}). We
restate the limits of this finding before discussing details:

\begin{itemize}
\item It is a refutation of the strong universalist reading of the
  canonical-cycle taxonomy. It is \emph{not} a denial that any
  cyclical structure exists in macroeconomic time series.
\item It depends on the empirical power of our protocol against
  realistic generative alternatives, which we report explicitly in
  Appendix~\ref{app:methods}. The AR(1) bootstrap is highly
  powerful against linear sinusoidal cycles (\(\geq 80\%\) at
  \(\mathrm{SNR}\geq 0.2\)) but weak against regime-switching and
  multifractal alternatives (3--26\% on MS-AR(1); 1--10\% on
  MFRW). The protocol's verdict in this paper is therefore
  scoped to the question of \emph{linear sinusoidal universality};
  whether a non-sinusoidal regime-switching cycle is present is a
  different empirical question that requires different tooling.
\item Some bona-fide cyclical components do pass the protocol: a
  3--5y inventory cycle is detected on dedicated sectoral
  inventory series (Section~\ref{sec:wen_replication}), and a
  Kuznets-band 15--25y residential construction cycle is detected
  on quarterly USA and GBR housing variables. These passages are
  consistent with \citet{wen2005} and \citet{klotz_neal1973} and
  we accept them.
\end{itemize}

\subsection{Verdict by cycle}

We report \(p_1\) (the AR(1) bootstrap \(p\)-value) and \(p_2\)
(the Theiler phase-randomised \(p\)-value, interpreted as a
coherence test, Section~\ref{sec:dual_null}) separately.

\paragraph{Kitchin (3--5y).}
On WB world GDP the band shows \(p_1 = 0.002\), \(p_2 = 0.031\):
power dominates AR(1) and phase coherence dominates the spectrum-
preserving null. Under the per-variable safeguard the result
collapses: of the 45 component variables on the WB world
composite, only seven individually satisfy \(p_1<0.05\) and of
those only two also satisfy \(p_2<0.05\), well below the
threshold of \(0.5\). The composite-positive result is therefore
recorded as a compositing artefact. On the sectoral panel,
following \citet{wen2005}, none of the eleven historical series
shows a Kitchin band passing Gate~\ref{gate:1}. We discuss the
non-trivial fact that \citet{wen2005} \emph{did} find a Kitchin
cycle on dedicated inventory series (which our sectoral panel
does not include) in Section~\ref{sec:wen_replication}.

\paragraph{Juglar (7--11y).}
On WB post-1960 the Juglar band fails Gate~\ref{gate:1} on all
income aggregates: typical \(p_1\) values are between 0.18 and
0.45. On the long-history panels (BoE pre-1945 and JST R6
restricted to 1870--1945) the band passes Gate~\ref{gate:1} on
G7 and ANGLO subsamples with \(p_1<0.01\), \(p_2<0.05\), then
collapses when the post-1945 window is added. The result
reproduces \citet{romer1999}'s Great Moderation finding under our
uniform protocol.

\paragraph{Kuznets (15--25y).}
Universally rejected on annual panels at Gate~\ref{gate:1}
(\(p_1>0.20\)) except the WLD composite which is borderline
(\(p_1=0.054\)). On quarterly USA and GBR, residential
construction variables pass the cascade up to Gate~\ref{gate:2}.
This is the residential-investment swing of
\citet{klotz_neal1973} and \citet{solomou1987}: a regional
construction cycle rather than a universal macroeconomic
periodicity.

\paragraph{Kondratieff (40--60y).}
On WB world GDP, HIC and OECD composites we observe
\(p_1\approx 0.001\). The per-variable safeguard eliminates
this result: zero of the 45 JST R6 variables passes
Gate~\ref{gate:1} on the Kondratieff band when tested
individually, and zero of the BoE Millennium UK variables does
either. On the JST R6 panel taken as a flat (variable, country)
grid of 810 cells, \(14\) cells pass Gate~\ref{gate:1} at
\(\alpha=0.05\). This is \emph{below} the expected count under
the joint null (\(\mathbb{E}[N_+\,|\,H_0]=40.5\) cells), and we
note this explicitly: 14/810 cells is consistent with the
hypothesis that there is no Kondratieff signal anywhere in JST
R6 and that the 14 passages are pure null sampling. Under
Benjamini-Hochberg correction at FDR 0.05, none of the 14
remains. We mention these numbers because a hostile reader could
choose to interpret 14 positive cells as evidence \emph{for} a
weak Kondratieff signal, and the honest accounting goes the
other way.

\subsection{Sensitivity to the window choice}

We report two windows for each panel: the full available window,
and a robustness window. For BoE Millennium, the robustness
window is 1700--1945 (pre-Bretton-Woods era); for JST R6,
1870--2007 (pre-financial-crisis); for WB, 1960--2007. The
qualitative verdict is invariant under each choice. The
exception is the small set of 14 JST Kondratieff cells discussed
above: 9 of 14 disappear when 2008--2020 is excluded, and 12 of
14 disappear when 2008--2020 is included but BH-FDR correction
is applied. The two perspectives agree that no Kondratieff
signal survives.

\subsection{Per-variable replication of \citet{wen2005}}
\label{sec:wen_replication}

To test composite-aggregation artefacts head-on we apply the
cascade to a sectoral panel of eleven historical series (rail
freight, pig iron, coal, cotton, wheat, etc.). The detailed
\(p_1, p_2\) values and survival rate per band are reported in
Appendix~\ref{app:per_variable}. The headline is that zero of
forty-four (\(11\times 4\)) cells survives Gate~\ref{gate:1}, on
both the full historical window and the post-1900 robustness
window. We are careful to note that this is not a reproduction
of \citet{wen2005}'s positive Kitchin finding: Wen worked on US
manufacturing inventory series specifically (NIPA inventory
accounts), which our sectoral panel does not include. The result
demonstrates that the sectoral-versus-composite distinction
\citet{wen2005} identified is real and that the composite
positive Kitchin signature we find on WB world GDP is an
aggregation artefact---but a reproduction of Wen's positive
result on inventory data is left to future work and would
require ingesting the NIPA inventory series under our protocol.

\subsection{What survives}

The cascade is not vacuous: it accepts a 3--5y inventory-style
cycle on dedicated inventory variables (consistent with the
original RBC literature \citep{wen2005}) and a regional
15--25y residential-construction cycle on post-1960 US and UK
quarterly data \citep{klotz_neal1973}. What it rejects is the
universalist reading: no single canonical period describes
macroeconomic fluctuations across heterogeneous panels.
